services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - opendluz_data:/app/data
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/memory.db

volumes:
  opendluz_data:
